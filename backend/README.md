# Backend

Django API for prescription persistence and drug interaction checking.

This scaffold intentionally does not implement the full application yet. The first implementation pass should add models, migrations, API views, serializers, and the Claude interaction service while keeping the scope limited to the two required screens.
